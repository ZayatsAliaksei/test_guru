<style type="text/css">
    #heading {
        text-align: center;
        padding: 10px; /* Поля вокруг текста */
        background: #39b5a2; /* Цвет фона */
        color: #fff; /* Цвет текста */
        width: 99%; /* Ширина слоя */
    }
</style>

<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title >header</title>
</head>
<body>
<header>
  <h1 id="heading">Test Guru</h1>
  <% if user_signed_in? %>
    <p class="nav user"><b><%= current_user.email %> <%= t('hello') %> </b>| <%= link_to  t('helpers.main.exit'), destroy_user_session_path, method: :delete %></p>
  <% end %>
</header>
</body>
</html>