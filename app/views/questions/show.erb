<!doctype html>
<html lang="en">

<body>
<h1><b>Question:</b></h1>

<p><%= @question.body %> <%= link_to "Delete", question_path(@question), method: :delete, data: {confirm: "Are you sure?"} %>
<a>  </a><%= link_to 'Edit', edit_question_path(@question) %></p>
<%= link_to 'Create new question', new_question_answer_path(@question) %><br>
<%= link_to 'Back', test_path(@question.test) %>
<p></p>
</body>
</html>