<h1><b>Question:</b></h1>
<p><%= @question.body %> <%= link_to "Delete", question_path(@question), method: :delete, data: { confirm: "Are you sure?" } %> | <%= link_to 'Edit', edit_question_path(@question) %></p>
<h3>Answers:</h3>
<%= link_to 'New Answer', new_question_answer_path(@question) %>
<ol> <%= render @question.answers %> </ol>
<%= link_to 'Back', test_path(@question.test) %>
<p></p>
